# P-3D AI共通運用ルール

`CLAUDE.md` は Codex、Claude Code、Gemini など全AI共通の主入口とする。`AGENTS.md` と `GEMINI.md` は本書へ収束する入口として維持し、同じ規則を重複記載しない。

## 1. 作業開始時の参照順

1. `CLAUDE.md`
2. `PROJECT_RULES.md`
3. `Docs/ARCHITECTURE.md`
4. `CHANGELOG.md`
5. 対象週の `Docs/CHAT_WORK_LOG_<YYMMDD>.md`

仕様が衝突した場合は、ユーザーの最新指示を最優先し、次に `PROJECT_RULES.md`、`Docs/ARCHITECTURE.md` の順で扱う。独断で仕様を変更せず、衝突内容を報告する。

## 2. コーディングとバグ修正

- 表面的な回避策を先に入れず、再現条件、一次原因、関連処理、修正が妥当な理由を確認する。
- バグ修正には可能な限り回帰検査を追加し、修正前に失敗理由、修正後に通過結果を確認する。
- 外部CDN、WASM、モデルなどの失敗で、カメラ・画像選択・手動操作まで停止させない。各機能の失敗境界を分離する。
- 外部ライブラリは実在する固定バージョンを使用し、URLのHTTP応答と配布物名を確認する。存在しない版、無指定の `latest`、未検証のURLを本番へ入れない。
- カメラ権限はiOS Safariのユーザー操作中に要求し、外部AIモジュールのダウンロード完了を待ってから要求してはならない。
- エラーを空の `catch` だけで隠さない。ユーザーが「カメラ権限」「カメラ起動」「顔検出準備」「顔未検出」を区別できる表示を維持する。
- 変更後は `python3 scripts/ci/web_regression_gate.py` を実行する。
- デスクトップブラウザの成功とiPhone Safari実機のカメラ・顔追跡成功を同一視しない。完了報告には検証環境と未検証範囲を記載する。

## 3. プライバシーと公開リポジトリ

- GitHub Pages公開URLは `https://yoshikawa303.github.io/P-3D/`、公開ブランチは `main` とする。
- 選択画像とカメラフレームはブラウザ内で処理し、明示的な仕様変更がない限り外部サーバーへアップロードしない。
- 著作権のある検証画像、個人画像、APIキー、資格情報、ブラウザデータをPublicリポジトリへ追加しない。
- 秘密情報をHTML、JavaScript、ログ、Git履歴へ保存しない。

## 4. 文書同期

- 機能や処理フローを変えた場合は `Docs/ARCHITECTURE.md` を更新する。
- ユーザー影響のある修正は `CHANGELOG.md` に記録する。
- AI入口・開発運用を変更した場合は `CLAUDE.md`、`AGENTS.md`、`GEMINI.md`、`PROJECT_RULES.md`、CI検査の整合を確認する。
- ルート直下にはアプリ本体、README、CHANGELOG、AI入口、プロジェクト規則を置き、作業記録や補助文書は `Docs/`、検査スクリプトは `scripts/ci/` に置く。

## 5. チャット作業履歴

- 相談、調査、実装、レビューを含む毎回の依頼と結果を、月曜始まりの週次ファイル `Docs/CHAT_WORK_LOG_<YYMMDD>.md` へ記録する。
- 受付時は調査・編集より先に `種別: 依頼内容` を追記する。ルール導入前から開始していた依頼は、導入時点で遡及記録する。
- 完了時は最終返信前に `種別: 作業結果` と概算作業時間を追記する。
- 見出しは `### YYYY-MM-DD HH:mm JST - <AI名とモデル> - 種別: <依頼内容|作業結果|経過|中断|再開> - <表題>` とする。
- 依頼内容はユーザーのテキストを原則そのまま fenced code block に記録する。添付画像や大量ログは `画像添付`、`大量ログ添付` などと記す。
- 作業結果は最終返信のMarkdown構造を保って記録し、モデル、動的切替の有無、検証、コミット／push結果、証明できない範囲を含める。

## 6. GitとGitHub Pages

- 作業開始時とコミット前にリポジトリルート、ブランチ、`git status --short --branch`、remoteを確認し、対象外差分を混在させない。
- コード、テスト、運用文書は論理単位ごとにコミットし、原則として直後に `main` へpushしてロールバック可能にする。
- push後はリモートのcommit SHAとPages配信内容を確認する。Pagesの反映待ちとアプリ不具合を混同しない。
- 対象ディレクトリがGit checkoutでない場合、許可なく `git init` しない。Git操作ができない理由と、リモート更新の有無を作業ログと最終報告へ記録する。
- 認証、ネットワーク、権限でpushできない場合は、未pushのcommit SHAと次に必要な操作を記録する。
- 履歴を書き換えるforce push、既存ファイル削除、タグ移動は、ユーザーの明示承認なしに行わない。

## 7. AIモデルと工数

- 作業前に、品質を保てる範囲で選択したモデル、対象工程、理由をチャットと作業ログへ記録する。
- 根本原因調査、ブラウザ互換性、複数ファイル横断の設計は上位モデルを優先する。定型文書転記や単純検査は軽量モデルを選択できる。
- 実行環境でモデル切替権限がない場合は、切替を装わずその事実を記録する。
- 作業後に実際のモデル、工数、動的切替の有無を記録する。

## 8. リリースと完了条件

1. 根本原因と修正範囲を説明できる。
2. `python3 scripts/ci/web_regression_gate.py` がPASSする。
3. ローカルHTTP表示で初期画面とJavaScriptエラーがないことを確認する。
4. GitHub `main` へ到達したcommit SHAを確認する。
5. Pages URLで修正版の配信を確認する。
6. iPhone Safari実機でカメラ許可、カメラ起動、顔追跡を確認する。実施していない場合は未検証と明記する。

